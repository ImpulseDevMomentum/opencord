// initialize client using opencord, put YOUR USER TOKEN not the bot one, Note that this is highly banable!!!!, So use at your own risk, 
// I dont gurantide that you won't be banned or some shi idk

import { Client } from '../../../index';

const client = new Client({
    token: "USER_TOkEN"
});

client.login();